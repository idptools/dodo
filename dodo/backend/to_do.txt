Stuff to do:
1. Change reading in of PDB to fot atomic positions
that then are used to get domains. Then build DODO Obj. 
This should let you avoid unnecessary calculations.

2. make a dodo_math.py module to hold common calculations.

3. Clean up dodo_structures, remove unnecessary stuff. There's a lot of it. 

4. Look through code, pull out functionality used multiple times and
make it into a function you can use iteratively. 

5. clean up the generate_alpha_carbon_points.py code!

6. (optional, possible future) - refactor all of the functionality
to assign the building of loops and IDRs to be within the classes.

7. Add in random walk as a possible option for generation, check if current method can do multiple  confromations

8. Change implementation to read in atoms, assign domains, then build up to avoid unnecessary compute

9. Consider random walk as a backup option

10. Check if coarse-graining at the beginning, if so, can be faster co CG on read then build. 

11. Implement functionality for assembly vs. not assembly

12. Implement multi conformations functionality. 

13. Integrate STARLING on to DODO dodo_structures